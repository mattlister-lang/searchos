# Secrets — never committed (see CLAUDE.md)
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Database dumps / backups — never committed
*.dump
*.sql.gz
backups/

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# OS noise
.DS_Store

# Claude Code local settings
.claude/settings.local.json
